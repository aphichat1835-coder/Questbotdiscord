name: Disabled final audit evidence helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary evidence helper is disabled"
