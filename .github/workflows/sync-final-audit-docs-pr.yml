name: Disabled PR documentation helper

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary PR documentation helper is disabled"
