[package]
name = "discord-cdp-launcher"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"
strip = "symbols"
